<div class="uk-flex uk-flex-middle">
  <a href="<%= course_path(@course, anchor: "enrollments", page: params[:page], scope: params[:scope]) %>" class="button grey"
      uk-tooltip="title: View All Lessons ; delay: 500 ; pos: top ;">
      <i class="icon-material-outline-arrow-back"></i> Back To Course: <%= @course.name %>
  </a>
</div>

<div class="uk-card-default mt-4 rounded">
    <div class="uk-card-header">
        <i class="icon-feather-plus mr-2"></i> Add A Enrollment for <%= @course.name %>
    </div>
    <div class="uk-card-body">
        <div uk-grid>
            <div class="uk-width-expand@m">
              <%= render "form" %>
            </div>
        </div>
    </div>
</div>
