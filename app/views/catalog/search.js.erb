<% if @courses.present? %>
  $("#course-list").html("<%= escape_javascript(render(partial: 'items', collection: @courses, as: :course)) %>");
<% else %>
  $("#course-list").html("<h3>No Courses Found</h3>");
<% end %>
